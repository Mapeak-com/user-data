@host = http://localhost:3000
@token = TEST_TOKEN

###
# @name CreateShare
POST {{host}}/api/urls
Authorization: Bearer {{token}}
Content-Type: application/json

{
    "title": "Test Route",
    "description": "A test route created by httpyac",
    "type": "Hiking",
    "difficulty": "Easy",
    "public": true,
    "osmUserId": "test-user",
    "start": {
        "lat": 32.0853,
        "lng": 34.7818
    }
}

?? status == 200
?? js JSON.parse(response.body).id.length > 0
?? js JSON.parse(response.body).title == "Test Route"

###
# @name GetShare
GET {{host}}/api/urls/{{CreateShare.id}}
Authorization: Bearer {{token}}

?? status == 200
?? js JSON.parse(response.body).id == "{{CreateShare.id}}"

###
# @name GetShareThumbnail - Should fail (404) initially
GET {{host}}/api/urls/{{CreateShare.id}}/thumbnail
Authorization: Bearer {{token}}

?? status == 404

###
# @name DeleteShare
DELETE {{host}}/api/urls/{{CreateShare.id}}
Authorization: Bearer {{token}}

?? status == 200
